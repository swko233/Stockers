<%= form_for(current_user.following_relationships.build, remote: true) do |f| %>
	<div><%= hidden_field_tag :following_id, @user.id %></div>
	<%= f.submit 'フォローする', class: "btn btn-follow" %>
<% end %>
